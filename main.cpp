#include "includes.hpp"

// IObject

int     main(void)
{
    Game _game;
    IncGraphics inc;

    while (1)
    {
        inc.DrawMap();
    }
    // OUTPUT(_game._init.getWidth());
    return (0);
}
